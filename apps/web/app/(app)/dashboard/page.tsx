export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-2xl font-semibold">Dashboard</h1>
      <p className="mt-2 text-[color:var(--muted)]">Tenant + Listing Agent widgets will come here.</p>
    </div>
  );
}
